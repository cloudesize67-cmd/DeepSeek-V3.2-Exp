name: base
channels:
  - defaults
  - conda-forge
  - pytorch
dependencies:
  - python=3.10
  - pip
  - pip:
    - torch
    - transformers
    - safetensors
    - fast_hadamard_transform
    - tilelang==0.1.6
